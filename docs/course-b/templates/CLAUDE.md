# CLAUDE.md 模板

> **使用说明**：复制此模板到你的项目根目录，根据项目实际情况填写。

## 项目概述

<!-- 用一句话说清楚这个项目是什么、做什么 -->

**项目名称**：[项目名称]

**项目目标**：[一句话描述项目目标]

**技术栈**：[列出主要技术栈]

## 目录结构

```
project-root/
├── src/                    # 源代码
│   ├── components/         # 组件
│   ├── utils/              # 工具函数
│   └── ...
├── tests/                  # 测试文件
├── docs/                   # 文档
└── ...
```

## 开发命令

### 安装依赖

```bash
[包管理器] install
```

### 运行测试

```bash
[测试命令]
```

### 启动开发服务器

```bash
[开发命令]
```

### 构建生产版本

```bash
[构建命令]
```

## 代码规范

### 命名规范

- [具体命名规范，如：组件使用 PascalCase，工具函数使用 camelCase]

### 代码风格

- [具体代码风格要求，如：使用 2 空格缩进，使用单引号]

### 注释要求

- [注释要求，如：公共 API 必须有 JSDoc 注释]

## 允许修改的范围

<!-- Agent 可以自由修改的文件和目录 -->

- [ ] `src/` 目录下的所有文件
- [ ] `tests/` 目录下的所有文件
- [ ] `docs/` 目录下的所有文件

## 禁止修改的范围

<!-- Agent 不能修改的文件和目录 -->

- [ ] `package.json`（除非明确要求）
- [ ] `.github/` 目录
- [ ] 配置文件（如 `tsconfig.json`, `.eslintrc.js`）

## 测试要求

### 测试覆盖率

- [测试覆盖率要求，如：新功能必须有单元测试，覆盖率不低于 80%]

### 测试命名

- [测试命名规范，如：测试文件以 `.test.ts` 结尾，测试用例以 `should` 开头]

## 部署说明

<!-- 如果需要部署，说明部署流程 -->

[部署相关说明]

## 注意事项

<!-- 其他需要 Agent 注意的事项 -->

- [注意事项 1]
- [注意事项 2]
- [注意事项 3]
