# Week 13 讲稿：Spec Coding——从「碰运气」到「按图施工」

**课程时长**：90 分钟
**授课对象**：非技术背景学生（如小林）
**教学目标**：理解 Spec Coding 核心理念，掌握规范驱动开发方法

* * *

## 课前准备（5 分钟）

### 开场白

大家好！欢迎来到课程 A 的第 13 周。

在开始之前，我想问大家一个问题：**有多少人在用 Claude Code 开发时，遇到过这样的情况——随口说一句需求，AI 做出来了，但后面改起来特别费劲，甚至要推倒重来？**

（举手互动，观察学生反应）

很好，我看到很多同学举手了。这很正常。其实小林也遇到过同样的问题。她用 Claude Code 做了几个月项目后，发现一个规律：**越是随口说一句就让 AI 开干，后面改起来越费劲；反而是那些她提前想清楚、写成文档的需求，AI 一次就能做对。**

直到她看到 OpenAI 研究员 Sean Grove 的演讲，才明白这背后有个名字——**Spec Coding**（规范驱动开发）。

今天这节课，我们就来学习这个改变 AI 编程方式的方法论。

### 课程目标

今天结束后，你们每个人都会：

1.  理解什么是 Spec Coding，以及为什么「规范本身就是代码」
2.  掌握三层规范结构的编写方法
3.  学会用 CLAUDE.md 和 Rules 目录管理项目规范
4.  掌握从 Vibe Coding 到 Spec Coding 的渐进式工作流

准备好了吗？让我们开始吧！

* * *

## Part 1：理解 Spec Coding（25 分钟）

### 1.1 什么是 Spec Coding？（5 分钟）

**\[展示 PPT：Spec Coding 定义\]**

首先，让我们明确一下什么是 Spec Coding。

**Spec Coding**，也叫 Spec-Driven Development（SDD），是一种以**规范文档作为开发核心产物**的方法论。

核心思路很简单：**先写清楚规范，再让 AI 根据规范生成代码。规范是 source of truth，代码只是规范的实现产物。**

**\[互动提问\]**
大家想想，这和我们之前学的 Vibe Coding 有什么区别？

（等待学生回应，引导到下一个观点）

**\[展示 PPT：小林的理解方式\]**

小林是这样理解的：

-   **Vibe Coding**：边走边看——想到哪说到哪，AI 跟着你的思路即兴发挥。快但容易跑偏。
-   **Spec Coding**：按图施工——先把设计图（规范）画好，AI 照着图纸精确实现。慢一点但一次到位。

这就像盖房子。Vibe Coding 是边盖边改，可能盖到一半发现地基不对；Spec Coding 是先画好图纸，按图施工，一次到位。

### 1.2 Sean Grove 的「The New Code」演讲（10 分钟）

**\[展示 PPT：Sean Grove 是谁\]**

现在让我给大家介绍一个人——Sean Grove。他是 OpenAI 的研究员，之前创办了 OneGraph（一个 GraphQL 开发工具公司，后被 Netlify 收购），目前在 OpenAI 从事 alignment reasoning 工作——帮助将高层意图转化为可执行的规范和评估标准。

2025 年，他在 AI Engineer World's Fair 上发表了一场题为 **"The New Code"** 的演讲，震动了整个开发者社区。

**\[展示 PPT：核心论点\]**

Grove 演讲的核心概念可以用一句话概括：

> **Code is a lossy projection of intent.**
> 代码是意图的有损投影。

**\[停顿，让学生思考\]**

什么意思？当你脑子里有一个想法，把它变成代码的过程中，大量的上下文信息会丢失——**为什么**要这样做、**权衡了哪些方案**、**考虑了什么约束**。最终的代码只保留了「怎么做」，却丢掉了「为什么这样做」。

**\[展示 PPT：举个例子\]**

让我给大家举个例子。假设你看到代码里有这样一行：

```javascript
if (retryCount > 5) { throw error; }
```

你只知道「重试超过 5 次就报错」，但你不知道：

-   为什么是 5 次而不是 3 次或 10 次？
-   这个数字是根据什么业务场景定的？
-   是否考虑过指数退避？
-   有没有测试过这个阈值的合理性？

这些「为什么」和「怎么来的」就是丢失的上下文——代码只保留了最终决策，却丢掉了决策过程。

**\[展示 PPT：编程的本质是沟通\]**

Grove 提出了一个看似简单却深刻的观点：

> "If you can communicate effectively, you can program."
> 如果你能有效沟通，你就能编程。

他认为，实际编码工作只占开发的 **10-20%**，剩下的 80% 是围绕需求和目标的**结构化沟通**——理解用户要什么、和团队对齐方案、定义验收标准、处理边界情况。

这意味着编程能力的核心不是掌握某种语言的语法，而是**把模糊的意图转化为精确描述的能力**。

**\[展示 PPT：写规范的人就是程序员\]**

这是 Grove 最具颠覆性的观点：

> "Whoever writes the spec — be it a PM, a lawmaker, an engineer, a marketer — is now the programmer."
> 无论是产品经理、律师、工程师还是市场人员，写规范的人就是程序员。

随着 AI 越来越擅长把规范转化为代码，**真正的编程工作**从「写代码」变成了「写规范」。谁能最精确地表达意图，谁就是最有价值的「程序员」。

**\[互动提问\]**
大家觉得这个观点怎么样？对于我们这些非技术背景的人来说，是不是一个好消息？

（让学生分享感受）

### 1.3 Vibe Coding vs Spec Coding 对比（10 分钟）

**\[展示 PPT：对比表格\]**

现在让我们对比一下 Vibe Coding 和 Spec Coding 这两种范式。

（逐行讲解表格内容）

小林回想起自己这几个月的开发经历，突然发现自己一直在两种模式之间切换，只是之前没意识到它们有名字。

**\[展示 PPT：小林的真实案例 - Vibe Coding\]**

让我给大家看一个真实的案例。小林上个月用 Vibe Coding 方式做通知功能：

```
第 1 轮：做一个通知功能（30 分钟）
第 2 轮：要支持已读未读（20 分钟）
第 3 轮：还要支持不同类型的通知（25 分钟）
第 4 轮：要能推送到手机（架构不对，推倒重来，2 小时）
第 5 轮：重新实现（1.5 小时）

总耗时：约 4.5 小时
代码质量：★★☆☆☆
```

结果：改了 4 轮，架构被反复推翻，代码越来越乱。最后她不得不推倒重来。

**\[展示 PPT：小林的真实案例 - Spec Coding\]**

同样是上个月，她用 Spec Coding 方式做退款功能：

```
第 1 步：写规范文档（20 分钟）
第 2 步：AI 生成代码（15 分钟）
第 3 步：小修小补（10 分钟）

总耗时：约 45 分钟
代码质量：★★★★☆
```

她先花 20 分钟写了一份规范文档，然后一次性让 Claude Code 实现。结果第一版就能用，只改了两个小细节。

**\[停顿，让数据沉淀\]**

大家看到差别了吗？写规范的 20 分钟看起来是「额外成本」，但实际上是投资。因为写规范的过程中，她把所有边界情况、数据结构、API 设计都想清楚了。AI 拿到这份规范后，就像拿到了施工图纸，一次就能做对。

**\[展示 PPT：两者并非对立\]**

但这里要强调一点：Vibe Coding 和 Spec Coding 并非对立关系。

聪明的工程师会用 Vibe Coding 作为第一步，生成规范的初始草稿。

小林现在的工作流是：

1.  先用 Vibe Coding 快速验证想法可行性
2.  把探索过程整理成规范
3.  用 Spec Coding 重新实现生产版本

**用 Vibe 的速度探索，用 Spec 的质量交付**——这是最佳实践。

* * *

## Part 2：Spec Coding 的三层规范结构（20 分钟）

### 2.1 三层结构概述（5 分钟）

**\[展示 PPT：三层结构\]**

小林看了几篇文章后，发现业界对规范的组织方式有个共识：分三层写，从抽象到具体。

**第一层：功能规范（What）**

-   用自然语言描述期望结果
-   回答「做什么」

**第二层：技术设计（How - 架构层）**

-   定义架构、数据模型、API
-   回答「怎么做」

**第三层：实现约束（How - 实现层）**

-   版本、框架、代码规范
-   回答「用什么做」

### 2.2 第一层：功能规范（5 分钟）

**\[展示 PPT：第一层示例\]**

第一层是功能规范，用自然语言描述期望结果。

让我给大家看一个用户认证功能的例子：

```markdown
## 用户认证功能

### 用户故事
- 作为新用户，我希望能通过邮箱注册账号
- 作为已注册用户，我希望能用邮箱和密码登录
- 作为忘记密码的用户，我希望能通过邮件重置密码

### 验收标准
- 注册时验证邮箱格式和密码强度
- 登录失败 5 次后锁定账号 15 分钟
- 密码重置链接 30 分钟内有效
```

注意，这一层完全不涉及技术细节，只描述「要做什么」和「怎么验证做对了」。

### 2.3 第二层：技术设计（5 分钟）

**\[展示 PPT：第二层示例\]**

第二层是技术设计，定义数据结构、架构模式、安全要求：

```markdown
## 技术设计

### 数据模型
- users 表：id, email, password_hash, created_at, locked_until
- sessions 表：id, user_id, token, expires_at

### API 设计
- POST /api/auth/register → 201 Created
- POST /api/auth/login → 200 OK + JWT
- POST /api/auth/reset-password → 202 Accepted

### 安全要求
- 密码使用 bcrypt 加密，cost factor ≥ 12
- JWT 有效期 15 分钟，refresh token 7 天
- 所有端点启用 rate limiting
```

这一层开始涉及技术决策，但还是架构层面的，不涉及具体实现。

### 2.4 第三层：实现约束（5 分钟）

**\[展示 PPT：第三层示例\]**

第三层是实现约束，版本要求、测试框架、文档标准：

```markdown
## 实现约束

### 技术栈
- Runtime: Node.js 20+
- Framework: Express 5
- ORM: Prisma
- Testing: Vitest

### 代码规范
- 使用 TypeScript strict mode
- 错误处理使用自定义 AppError 类
- 所有 API 端点需要 JSDoc 注释
```

这一层是最具体的，告诉 AI 用什么工具、什么版本、什么规范。

**\[展示 PPT：小林的经验\]**

小林的经验是：不用每次都写三层。

-   **快速原型**：只写第一层就够
-   **团队协作**：至少要写到第二层
-   **生产级项目**：才需要第三层

她现在的习惯是：先写第一层验证需求，确认方向后再补第二层，最后根据项目规模决定要不要第三层。

* * *

## Part 3：在 Claude Code 中实践 Spec Coding（30 分钟）

### 3.1 Claude Code 的「万物皆 Markdown」哲学（5 分钟）

**\[展示 PPT：万物皆 Markdown\]**

在深入实践之前，先理解 Claude Code 的底层哲学：**万物皆 Markdown**。

Claude Code 的设计中，所有过程记载、信息传递、甚至与模型的对话，都可以是 Markdown：

-   **CLAUDE.md**：项目规范的 Markdown 文档
-   **.claude/rules/**：分层规范的 Markdown 文件集合
-   **specs/**：功能需求的 Markdown 描述
-   **对话历史**：Claude Code 的对话记录本身就是 Markdown 格式

这正是 Spec Coding 的内核：**规范本身就是代码**。当你用 Markdown 写下需求、设计、验收标准时，你已经在写「代码」了——AI 会读取这些 Markdown，然后生成真正的代码实现。

### 3.2 第一步：用 CLAUDE.md 建立项目规范（8 分钟）

**\[展示 PPT：CLAUDE.md 的作用\]**

`CLAUDE.md` 就是你项目的「活规范」。每次 Claude Code 启动时都会读取它，相当于给 AI 一份持久的项目说明书。

在前面的课程中，我们学过如何创建 `CLAUDE.md`。在 Spec Coding 的语境下，它的角色更加重要——**它不只是配置文件，而是项目规范的入口**。

**\[展示 PPT：一个好的 CLAUDE.md 应该包含什么\]**

让我给大家看一个电商项目的 CLAUDE.md 示例：

```markdown
# 电商项目规范

## 项目定位
面向中小商家的 SaaS 电商平台，支持多店铺、多支付渠道。

## 架构决策
- 前后端分离，API-first 设计
- 后端微服务架构，服务间通过消息队列通信
- 数据库读写分离

## 核心约束
- 所有金额使用整数（分）存储，避免浮点精度问题
- 订单状态机严格遵循：待支付 → 已支付 → 已发货 → 已完成
- 支付相关接口必须幂等

## 开发规范
- 使用 TypeScript strict mode
- API 响应格式：成功 { data, pagination? }，失败 { error: { code, message } }
- 所有数据库操作必须有事务保护
```

**\[互动\]**
大家注意到了吗？这份文档不是一次写完就不动了。

小林的习惯是：每次遇到 AI 犯同样的错误，就把对应的规则加进去。比如有次 AI 把金额用 float 存储导致精度问题，她就在「核心约束」里加了一条「金额用整数（分）」。久而久之，这份文档就成了项目的「避坑指南」。

### 3.3 第二步：用 Rules 目录管理分层规范（8 分钟）

**\[展示 PPT：为什么需要 Rules 目录\]**

当项目变大，单个 `CLAUDE.md` 不够用。这时候用 `.claude/rules/` 目录来组织分层规范。

为什么需要 Rules 目录？

-   **按需加载**：只在编辑相关文件时加载对应规范，节省 token
-   **职责分离**：前端规范、后端规范、数据库规范各管各的
-   **团队协作**：不同成员可以维护不同领域的规范

**\[展示 PPT：目录结构示例\]**

```
.claude/rules/
├── 00-architecture.md      # 架构规范（全局）
├── 01-security.md           # 安全规范（全局）
├── 10-api-design.md         # API 设计规范
├── 11-frontend-patterns.md  # 前端模式规范
├── 12-database.md           # 数据库规范
└── 20-testing.md            # 测试规范
```

**\[展示 PPT：一个具体的 Rules 文件示例\]**

让我给大家看一个 API 设计规范的例子：

```markdown
---
globs:
  - "src/api/**/*.ts"
  - "src/services/**/*.ts"
---

# API 设计规范

## 路由设计
- RESTful 风格，使用名词复数：/api/v1/orders
- 嵌套资源最多两层：/api/v1/users/123/orders

## 响应格式
- 成功：{ data, pagination? }
- 错误：{ error: { code, message, details? } }

## 必须遵守
- 所有写操作需要认证
- 列表接口必须支持分页
- 敏感操作记录审计日志
```

注意 frontmatter 中的 `globs` 字段——它告诉 Claude Code：只有在编辑 `src/api/` 或 `src/services/` 下的 TypeScript 文件时，才加载这份规范。这样既保证了规范的精准性，又不会浪费 token。

### 3.4 第三步：用 /plan 实现四阶段工作流（9 分钟）

**\[展示 PPT：四阶段工作流\]**

Spec Coding 的标准工作流是四阶段循环：**Specify → Plan → Tasks → Implement**

Claude Code 的 `/plan` 命令天然支持这个流程。

**阶段一：Specify（定义规范）**

先写清楚要做什么，不急着写代码。小林现在的习惯是先创建一个 `specs/` 目录，把功能规范写进去。

**\[演示操作\]**

```
/plan
我需要实现订单退款功能，规范如下：

功能需求：
- 用户可以在发货前申请全额退款
- 发货后 7 天内可以申请退货退款
- 退款需要管理员审批

验收标准：
- 退款金额不能超过订单实付金额
- 退款状态机：申请中 → 审批通过 → 退款中 → 已退款
- 退款完成后恢复库存
- 全程记录操作日志
```

**阶段二：Plan（技术规划）**

Claude 会基于你的规范生成技术方案：

```
📋 退款功能实施计划

1. 数据模型设计
   - 创建 refunds 表
   - 添加订单状态机的退款相关状态

2. API 设计
   - POST /api/orders/:id/refund - 申请退款
   - PUT /api/refunds/:id/approve - 审批退款
   - GET /api/refunds - 退款列表

3. 业务逻辑
   - 退款资格校验
   - 退款金额计算
   - 库存恢复逻辑

4. 集成
   - 对接支付渠道退款接口
   - 发送退款通知
```

**阶段三：Tasks（任务拆解）**

将计划拆分为可独立执行的小任务，每个任务有明确的完成标准。Claude Code 会自动帮你拆解，你也可以手动调整。

**阶段四：Implement（逐步实现）**

按任务逐个实现，每完成一个就验证一次。小林的习惯是让 Claude Code 做完一个任务就暂停，她确认没问题后再继续下一个。

**\[展示对话示例\]**

```
小林：按照刚才的计划实现退款功能，每完成一个模块暂停一下，我确认后再继续

Claude：好的，我先从数据模型开始。
[创建 refunds 表的 migration 文件]
[更新 Order 模型，添加退款相关状态]

数据模型已完成，请确认：
- refunds 表包含所有必要字段
- 订单状态机已更新
- 外键关系正确

确认无误后我继续实现 API 层。
```

这种「做一步、停一步」的节奏特别适合复杂功能。

* * *

## Part 4：实战演示（20 分钟）

### 4.1 场景设定（2 分钟）

**\[展示 PPT：场景设定\]**

现在让我们通过一个完整的案例演示 Spec Coding 的全流程。

小林的电商项目需要一个通知系统，用来推送订单状态、促销活动、系统公告等消息。她决定用 Spec Coding 方式实现。

### 4.2 步骤 1：编写功能规范（5 分钟）

**\[展示 PPT：功能规范\]**

她先创建 `specs/notification-system.md`：

```markdown
# 用户通知系统规范

## 功能需求

### 核心功能
1. 支持站内通知、邮件通知、推送通知三种渠道
2. 通知类型：系统公告、订单状态、促销活动、安全提醒
3. 用户可以按渠道和类型配置通知偏好
4. 支持已读/未读状态，支持批量标记已读

### 用户故事
- 作为用户，我希望在订单状态变化时收到通知
- 作为用户，我希望能关闭不想要的通知类型
- 作为用户，我希望能一键标记所有通知为已读
- 作为管理员，我希望能群发系统公告

## 验收标准
- 未读通知数实时更新
- 通知列表支持无限滚动
- 推送通知延迟 < 3 秒
- 偏好设置变更立即生效
```

**\[互动\]**
大家看到了吗？这一层完全是用自然语言描述的，没有任何技术细节。

### 4.3 步骤 2：补充技术设计（5 分钟）

**\[展示 PPT：技术设计\]**

继续在同一个文件中补充技术层面的设计：

```markdown
## 技术设计

### 数据模型
- notifications 表：
  - id, user_id, type, channel, title, content
  - is_read, created_at, read_at
- notification_preferences 表：
  - user_id, type, channel, enabled

### API 设计
- GET /api/notifications?type=&is_read= - 获取通知列表（分页）
- PUT /api/notifications/:id/read - 标记已读
- PUT /api/notifications/read-all - 全部标记已读
- GET /api/notification-preferences - 获取偏好设置
- PUT /api/notification-preferences - 更新偏好设置

### 架构决策
- 使用消息队列异步发送通知，避免阻塞主流程
- 站内通知存储在数据库，邮件和推送只记录发送日志
- WebSocket 实时推送未读数变化
```

### 4.4 步骤 3：在 Claude Code 中执行（5 分钟）

**\[展示 PPT：执行指令\]**

现在她打开 Claude Code，引用这份规范：

```
@specs/notification-system.md
按照这份规范实现用户通知系统。
先从数据模型开始，然后实现 API，最后做前端组件。
每完成一个模块暂停一下，我确认后再继续。
```

**\[演示对话流程\]**

（模拟 Claude 的回应和小林的确认过程）

### 4.5 步骤 4：结果对比（3 分钟）

**\[展示 PPT：结果对比表格\]**

小林做完后对比了一下数据：

指标

Vibe Coding（之前）

Spec Coding（这次）

规范编写时间

0 分钟

25 分钟

开发时间

4.5 小时

1 小时

返工次数

4 次

0 次

代码质量

★★☆☆☆

★★★★☆

文档完整度

无

规范即文档

她的感受：

> 写规范的 25 分钟看起来是「额外成本」，但实际上是投资。因为写规范的过程中，我把所有边界情况、数据结构、API 设计都想清楚了。AI 拿到这份规范后，就像拿到了施工图纸，一次就能做对。

* * *

## Part 5：混合策略与最佳实践（10 分钟）

### 5.1 什么时候用 Vibe Coding（3 分钟）

**\[展示 PPT：Vibe Coding 适用场景\]**

小林现在明白了：行业共识并非「抛弃 Vibe Coding」，而是**根据场景选择合适的方式**。

**快速探索场景：**

-   验证一个想法是否可行（30 分钟内的原型）
-   探索不熟悉的技术或框架
-   Hackathon 或内部 demo
-   一次性脚本或工具

**小林的例子：** 她想试试能不能用 WebSocket 实现实时通知，就直接让 Claude Code 写了个最简单的 demo。10 分钟跑通了，证明可行，然后再用 Spec Coding 做生产版本。

### 5.2 什么时候用 Spec Coding（3 分钟）

**\[展示 PPT：Spec Coding 适用场景\]**

**生产级开发场景：**

-   生产级功能开发
-   多人协作的项目
-   需要长期维护的代码
-   涉及安全、支付、数据等敏感领域
-   API 设计和系统集成

**小林的例子：** 支付模块、用户认证、订单系统这些核心功能，她现在都用 Spec Coding。因为这些模块出错的代价太高，必须一次做对。

### 5.3 推荐的渐进式工作流（4 分钟）

**\[展示 PPT：三步走\]**

小林现在的标准流程是三步走：

**① Vibe 探索** 用 Vibe Coding 快速验证想法，不写规范，不管代码质量：

```
小林：做一个简单的通知弹窗，看看效果
Claude：[10 分钟生成一个基础原型]
```

**② 提炼规范** 验证可行后，把探索中的发现整理成规范。你甚至可以让 AI 帮你：

```
小林：基于我们刚才做的通知功能原型，
帮我整理一份正式的功能规范文档，
包括数据模型、API 设计和验收标准
```

**③ Spec 重建** 基于规范，用 Spec Coding 方式重新实现生产级版本：

```
@specs/notification-system.md
按照这份规范从零实现，不要参考之前的原型代码
```

**这个流程的好处：** 用 Vibe Coding 的速度验证方向，用 Spec Coding 的质量交付产品。小林发现这样做的成功率最高——既不会在错误方向上浪费时间写规范，也不会因为没规范而反复返工。

* * *

## 本章回顾（5 分钟）

### 6.1 学习进度检查

**\[展示 PPT：ProgressTracker\]**

让我们回顾一下今天学到了什么：

✅ 理解了 Spec Coding 核心理念（代码是意图的有损投影，规范才是真正的「新代码」） ✅ 掌握了三层规范结构（功能规范、技术设计、实现约束） ✅ 会用 CLAUDE.md 和 Rules 目录管理项目规范 ✅ 掌握了 Specify → Plan → Tasks → Implement 四阶段工作流 ✅ 理解了混合策略（什么时候用 Vibe、什么时候用 Spec） ✅ 学会了渐进式工作流（Vibe 探索 → 提炼规范 → Spec 重建）

### 6.2 自测题

**\[展示 PPT：自测题\]**

现在让我们通过几个问题检验一下学习效果：

**1\. 什么叫「代码是意图的有损投影」？举一个你自己项目中的例子。**

**2\. Vibe Coding 和 Spec Coding 各自适合什么场景？如果你要做一个支付模块，你会选哪种方式？为什么？**

**3\. 描述一下 Specify → Plan → Tasks → Implement 四阶段的具体操作。在 Claude Code 中，你会用什么命令和工具来实现这个流程？**

**4\. 一份好的功能规范应该回答哪 8 个问题？**

**5\. 描述一下「Vibe 探索 → 提炼规范 → Spec 重建」的渐进式工作流。什么时候应该用这个流程？**

（让学生思考和讨论）

### 6.3 本周作业

**\[展示 PPT：作业要求\]**

**必做**：

-   选择一个你正在做的功能，用 Spec Coding 方式重新实现
-   创建一份功能规范文档（至少包含第一层和第二层）
-   记录开发过程和时间对比，看看是否比 Vibe Coding 更高效

**选做**：

-   为你的项目创建 CLAUDE.md 文件
-   尝试用 Rules 目录管理分层规范
-   分享你的规范文档到课程讨论区

**截止时间**：下周上课前

* * *

## 下周预告（3 分钟）

**\[展示 PPT：下周预告\]**

下周我们要学什么？

**Week 12：Agent Teams - AI 团队协作开发**

掌握了 Spec Coding 之后，小林开始思考：能不能让多个 AI 实例像真正的开发团队一样协同工作？一个负责前端、一个负责后端、一个负责测试？

下周我们进入 Agent Teams——让 AI 团队协作开发，把「一个人的 AI 助手」升级为「AI 开发团队」。

* * *

## Q&A（5 分钟）

**\[展示 PPT：Q&A\]**

现在是提问时间。大家有什么问题吗？

（回答学生问题）

* * *

## 结束语（2 分钟）

**\[展示 PPT：结束语\]**

今天我们学习了 Spec Coding——从「碰运气」到「按图施工」的方法论。

我希望大家记住三句话：

> 规范本身就是代码 写清楚规范，AI 就能一次做对 用 Vibe 的速度探索，用 Spec 的质量交付

不要担心写规范会浪费时间。那 20 分钟的投入，会在后续开发中十倍百倍地回报给你。

期待下周看到你们的规范文档！

谢谢大家！

* * *

## 课后反思（教师用）

### 观察要点

-   学生是否理解了「代码是意图的有损投影」这个核心概念？
-   学生是否能区分 Vibe Coding 和 Spec Coding 的适用场景？
-   学生是否掌握了三层规范结构的编写方法？
-   学生是否能在 Claude Code 中实践 Spec Coding 工作流？

### 改进建议

-   如果学生对「有损投影」概念理解困难，可以增加更多实际代码示例
-   如果时间不够，可以压缩「混合策略」部分，重点放在核心工作流上
-   如果学生反应热烈，可以增加更多实战演示环节

### 下周准备

-   提醒学生完成本周作业（创建规范文档）
-   准备 Week 12 的 Agent Teams 演示项目
-   收集本周作业，选出优秀规范文档在下周展示
