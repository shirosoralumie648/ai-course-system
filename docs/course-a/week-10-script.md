# Week 10 讲稿：Skills——给 Claude Code 装上「技能包」

**课程时长**：90 分钟
**授课对象**：已学习 Claude Code 基础的学生
**教学目标**：让学生理解 Skills 的价值，并创建至少 3 个自定义 Skill

* * *

## 课前准备（5 分钟）

### 开场白

大家好！欢迎来到 Week 09 的课程。

在开始之前，我想问大家一个问题：**有多少人在使用 Claude Code 时，发现自己总是在重复同样的指令？**

比如：

-   每次都要说「代码风格要符合 ESLint 规范」
-   每次都要说「提交信息要用 Conventional Commits 格式」
-   每次都要说「测试用例要覆盖边界情况」

（举手互动，观察学生反应）

很好，我看到很多同学举手了。这很正常。但今天，我要告诉你们一个好消息：**这些重复的指令，可以一次性写成「技能包」，让 Claude Code 永久记住。**

这就是我们今天要学的 **Skills**。

### 课程目标

今天结束后，你们每个人都会：

1.  理解 Skills 的本质和价值
2.  会安装和使用社区 Skills
3.  能创建至少 3 个自定义 Skills
4.  知道如何将 Skills 用于团队协作

准备好了吗？让我们开始吧！

* * *

## Part 1：认识 Skills（20 分钟）

### 1.1 小林的困扰（5 分钟）

**\[展示 PPT：小林的故事\]**

让我先给大家讲一个故事。

小林用了几周 Claude Code，她发现自己每次都要重复解释：

-   「代码要符合什么风格」
-   「提交信息要怎么写」
-   「测试用例要覆盖哪些情况」

每次开始新对话，她都要重新解释一遍。这让她感到很疲惫。

**\[互动提问\]**
大家有没有类似的经历？

（等待学生回应）

直到有一天，她听说有个叫 Skills 的东西，可以把这些要求写成「技能包」，让 Claude Code 一次学会、永久记住。

那一刻她意识到：**AI 编程不只是对话，更是知识沉淀。**

### 1.2 什么是 Skills？（5 分钟）

**\[展示 PPT：Skills 定义\]**

**Claude Code Skills** 是一种将专业知识、工作流程和最佳实践打包成「可复用技能包」的功能。

让我用一个比喻来解释：

**如果 Claude Code 是一个新员工，那 Skills 就是公司的「操作手册」。**

-   代码审查怎么做？翻手册
-   Git 提交信息怎么写？翻手册
-   测试用例怎么生成？翻手册

新员工不用每次问，翻手册就行。

**\[停顿，让学生理解\]**

这就是 Skills 的本质：**把知识和流程固化下来，让 AI 可以反复使用。**

### 1.3 为什么需要 Skills？（5 分钟）

**\[展示 PPT：没有 Skills 之前的问题\]**

在没有 Skills 之前，我们遇到这些问题：

1.  **重复指令**：每次都要解释代码风格、提交规范
2.  **知识无法沉淀**：团队成员各自的使用经验无法共享
3.  **标准不统一**：不同的人用 Claude，结果可能完全不同
4.  **效率低下**：常见的任务每次都要从头解释

**\[互动提问\]**
大家在使用 Claude Code 时，有没有遇到这些问题？

（让几个学生分享）

Skills 解决了这些问题，让 Claude 变成一个「有经验的团队成员」——它知道你的项目规范、工作流程和最佳实践。

### 1.4 Skills 正在成为必备技能（5 分钟）

**\[展示 PPT：社区热度\]**

Skills 不是小众功能，而是整个 AI 编程社区都在推的方向：

-   **社区热度高**：GitHub 上 OpenSkills 项目已收获 7.2k stars
-   **官方支持**：Anthropic 官方维护 Skills 仓库，Vercel 推出 find-skills 工具
-   **实用性强**：从代码审查、Git 操作到视频制作、PPT 生成，覆盖多种场景
-   **效率提升**：一次配置，反复使用，让 Claude 真正成为你的「数字员工」

**\[停顿\]**

所以，学会 Skills 不只是学一个功能，而是掌握一种新的工作方式。

* * *

## Part 2：快速开始：安装第一个 Skill（25 分钟）

### 2.1 第一步：安装 find-skills（8 分钟）

**\[展示 PPT：find-skills\]**

在开始使用 Skills 之前，我强烈推荐先安装 `find-skills`。

**find-skills 是什么？**

简单来说，find-skills 就像是 AI Agent 的「应用商店搜索器」。当你需要完成某个任务但本地没有对应的 Skill 时，它会自动帮你搜索并推荐最合适的 Skill。

**\[演示操作\]**

现在请大家跟着我一起做。打开终端，输入：

```bash
npx skills add vercel-labs/skills@find-skills -g -y
```

**\[等待学生操作，巡视确认\]**

安装完成后，你就可以直接告诉 Claude 你的需求，它会通过 find-skills 自动搜索相关技能。

**\[展示 PPT：使用示例\]**

比如，你可以说：

```
我需要做一个 React 组件的性能优化，
帮我找找有什么技能可以用
```

Claude 会自动搜索并推荐最合适的 Skill。

**\[停顿\]**

为什么推荐先装 find-skills？

-   **没有 find-skills 之前**：手动在 GitHub 搜索相关技能 → 逐个复制、安装、配置 → 反复调试适配
-   **有了 find-skills 之后**：一句话描述需求 → AI 自动搜索最匹配的技能 → 一键安装，立即可用

### 2.2 第二步：体验 Remotion 视频制作（8 分钟）

**\[展示 PPT：Remotion\]**

安装 find-skills 后，让我们用它来搜索并安装第一个好玩的 Skill —— Remotion 视频制作工具。

**\[演示操作\]**

在 Claude Code 中输入：

```
帮我找找 Remotion 相关的技能，我想做视频
```

Claude 会通过 find-skills 搜索，推荐 `remotion-dev/skills`。

然后我们安装它：

```bash
npx skills add remotion-dev/skills -g
```

**\[等待学生操作\]**

安装完成后，我们来做个炫酷的文字动画视频。在 Claude Code 中输入：

```
用 Remotion 做一个视频：
- 1920x1080，5 秒
- 一行文字 "Hello World" 从左边飞进来
- 同时带旋转和缩放效果
- 背景是渐变色
```

**\[等待 Claude 生成代码\]**

大家看到了吗？Claude 生成了完整的 Remotion 代码，你可以运行它看到动画效果。

这就是 Skills 的魅力：**装上技能包，Claude 就能做视频了。**

### 2.3 第三步：frontend-design 让界面变好看（9 分钟）

**\[展示 PPT：frontend-design\]**

小林的前端页面总是被吐槽「看起来很土」，她用 find-skills 找到了解决方案。

**\[演示操作\]**

在 Claude Code 中输入：

```
我的网页看起来很土，而且加载很慢，
帮我找找有什么技能可以用
```

Claude 会推荐 **anthropics/skills/frontend-design**（官方技能）。

这个技能专门解决 AI 生成的界面「看起来很土」的问题，让 Claude 设计出：

-   独特的视觉风格（避开千篇一律的「AI 模板感」）
-   专业的配色和字体
-   流畅的动画效果
-   生产级别的代码质量

**\[安装并使用\]**

```bash
npx skills add anthropics/skills/frontend-design -g
```

然后在 Claude Code 中说：

```
帮我重新设计这个页面，要看起来很专业，别像 AI 生成的
```

**\[展示效果\]**

大家看到区别了吗？同样是 AI 生成的代码，但有了 frontend-design 这个技能包，生成的界面质量完全不一样。

* * *

## Part 3：Skills 的核心概念（15 分钟）

### 3.1 Skills 是什么？（5 分钟）

**\[展示 PPT：Skills 结构\]**

体验过几个 Skills 后，让我们深入理解它的工作原理。

**Skills 是存储在文件系统中的「技能包」**，包含：

```
my-skill/
├── SKILL.md          # 必需：技能定义文件
├── scripts/          # 可选：辅助脚本
├── templates/        # 可选：输出模板
├── references/       # 可选：参考文档
└── examples/         # 可选：示例文件
```

最核心的是 **SKILL.md** 文件，它定义了这个技能是什么、怎么用。

### 3.2 Skills vs 提示词（5 分钟）

**\[展示 PPT：对比表格\]**

你可能会有疑问：Skills 和直接给 Claude 发提示词有什么区别？

让我给大家看一个对比：

传统提示词方式

使用 Skills

每次对话都要重复说

创建一次反复用

存在对话历史中，占用 Token

按需加载，节省 Token

无法在会话间共享

可以在团队中共享

难以版本控制

可以用 Git 管理

**核心区别**：

-   提示词是临时的，存在对话历史中
-   Skills 是持久化的，写一次反复用

### 3.3 Skills 的两种类型（5 分钟）

**\[展示 PPT：两种类型\]**

Skills 有两种类型：

**全局 Skills（个人）**：

-   存放位置：`~/.claude/skills/`
-   作用范围：所有项目
-   适用场景：个人通用技能（代码风格、Git 习惯）

**项目 Skills（团队）**：

-   存放位置：`项目目录/.claude/skills/`
-   作用范围：当前项目
-   适用场景：团队共享、项目特定规范

**\[互动提问\]**
大家觉得什么样的技能应该放全局，什么样的应该放项目级？

（让学生讨论）

小林的选择策略：

-   个人习惯（比如她喜欢的代码注释风格）放全局
-   团队规范（比如公司的 Git 提交规范）放项目级

这样换项目时不会把个人习惯带过去，也不会丢掉团队标准。

* * *

## Part 4：如何创建自己的 Skills（20 分钟）

### 4.1 方法一：直接让 Claude 帮你创建（8 分钟）

**\[展示 PPT：方法一\]**

这是最简单的方式，直接用自然语言告诉 Claude 你的需求。

**\[演示操作\]**

在 Claude Code 中输入：

```
请帮我创建一个名为「format-code」的 skill，
功能是自动格式化代码。

要求：
1. 自动检测编程语言类型
2. 应用对应的格式化规则
3. 返回格式化前后的 diff
```

**\[等待 Claude 生成\]**

大家看到了吗？Claude 会自动：

1.  创建目录结构
2.  生成 SKILL.md 文件
3.  填写 YAML frontmatter
4.  编写技能内容

**\[展示生成的文件\]**

技能已创建完成！你可以通过以下方式使用：

-   直接输入：`/format-code`
-   或者说：「帮我格式化这段代码」

**适用场景**：

-   快速创建简单技能
-   你知道要什么，但不熟悉 SKILL.md 格式
-   想要快速迭代和修改

### 4.2 方法二：使用 skill-creator（7 分钟）

**\[展示 PPT：方法二\]**

skill-creator 是一个专门用来创建 Skills 的工具，会引导你一步步完成。

**\[演示操作\]**

首先安装：

```bash
npx skills add anthropics/skills@skill-creator -g
```

然后使用：

```
/skill-creator
```

**\[展示引导流程\]**

skill-creator 会引导你：

1.  技能名称
2.  功能描述
3.  使用场景
4.  执行步骤
5.  生成 SKILL.md 草稿
6.  创建测试用例
7.  运行评估并优化

**适用场景**：

-   创建复杂的技能
-   需要规范的创建流程
-   想要测试和验证技能

### 4.3 如何写好需求（5 分钟）

**\[展示 PPT：好需求 vs 坏需求\]**

无论用哪种方法，写好需求都很重要。

**好的需求描述**：

```
创建一个「git-commit」skill，功能是自动提交代码。

执行步骤：
1. 检查有哪些文件被修改
2. 生成符合 Conventional Commits 规范的提交信息
3. 执行 git commit
4. 询问是否需要 push

注意事项：
- 提交前先检查是否有敏感信息
- 不要提交 dist/node_modules/ 等目录
```

**不好的需求描述**：

```
帮我写一个提交代码的 skill
```

太模糊了，Claude 不知道具体要做什么。

**\[停顿\]**

小林的经验：把需求写得像「给人类同事的工作交接文档」——步骤清晰、注意事项明确、边界条件说清楚。这样 Claude 生成的 Skill 才能真正可用。

* * *

## Part 5：实战：创建三个常用 Skills（20 分钟）

### 5.1 实战 1：代码审查 Skill（7 分钟）

**\[展示 PPT：代码审查 Skill\]**

小林根据自己的日常工作，创建了第一个 Skill：代码审查。

**需求分析**：

小林每次审查代码都要检查：代码风格、安全性、测试覆盖率。与其每次重复说，不如做成 Skill。

**\[演示操作\]**

在 Claude Code 中输入：

```
请帮我创建一个名为「review-pr」的 skill。

功能：审查 Pull Request 的代码质量、安全性和测试覆盖率

执行流程：
1. 代码风格检查
   - 代码是否符合团队规范
   - 命名是否清晰
   - 注释是否充分

2. 安全性检查
   - 是否有安全漏洞
   - 敏感信息是否暴露
   - 输入验证是否完善

3. 测试检查
   - 是否有足够的测试
   - 测试用例是否覆盖边界情况
   - 测试是否可运行

4. 总体评价
   - 优点是什么
   - 需要改进的地方
   - 建议是否批准合并

输出格式：请以清晰的结构输出审查结果，使用列表形式。

触发关键词：PR、review、代码审查
```

**\[等待 Claude 生成\]**

**使用方式**：

```
/review-pr
请审查当前分支的 PR
```

或者直接说：

```
帮我审查这个 PR
```

### 5.2 实战 2：Git 自动提交 Skill（7 分钟）

**\[展示 PPT：Git 自动提交 Skill\]**

**需求分析**：

小林每次提交代码都要：检查修改 → 写提交信息 → 确认没有敏感信息 → 提交。这个流程完全可以自动化。

**\[演示操作\]**

在 Claude Code 中输入：

```
请帮我创建一个名为「git-commit」的 skill。

功能：自动检测修改、生成提交信息并提交代码

执行流程：
1. 检查修改
   运行 git status 查看修改的文件
   运行 git diff 查看具体改动

2. 生成提交信息
   分析改动的性质
   生成符合 Conventional Commits 格式的提交信息
   格式：type(scope): description

3. 安全检查
   检查是否有敏感信息（密钥、密码、token）
   检查是否包含了不该提交的目录

4. 确认后执行
   显示提交信息供确认
   执行 git add 和 git commit
   询问是否需要 push

注意事项：
- 不要提交 node_modules/、dist/、.next/ 等目录
- 提交前先运行测试确保代码可用
- 提交信息要清晰说明改动内容

触发关键词：commit、提交代码
```

**使用方式**：

```
/git-commit
```

或者：

```
帮我提交代码
```

### 5.3 实战 3：测试生成 Skill（6 分钟）

**\[展示 PPT：测试生成 Skill\]**

**需求分析**：

小林写完功能后总是忘记写测试，或者不知道该测什么。这个 Skill 可以自动生成测试用例。

**\[演示操作\]**

在 Claude Code 中输入：

```
请帮我创建一个名为「gen-test」的 skill。

功能：为代码自动生成单元测试，确保功能正确性

工作流程：
1. 分析代码
   - 理解函数/类的功能
   - 识别输入和输出
   - 找出边界情况

2. 生成测试
   - 使用合适的测试框架
   - 覆盖正常情况
   - 覆盖边界情况
   - 覆盖异常情况

3. 验证测试
   - 确保测试可以运行
   - 确保测试能检测到问题
   - 不要过度模拟实现

测试框架：
- JavaScript/TypeScript：Jest 或 Vitest
- Python：pytest
- Go：testing 包

输出格式：先输出测试代码，然后说明如何运行测试。

触发关键词：测试、test、单元测试
```

**使用方式**：

```
/gen-test
为 src/utils.ts 生成单元测试
```

**\[停顿\]**

小林的实战总结：

创建这三个 Skills 后，她的工作效率提升了至少 30%。以前每次代码审查要花 10 分钟解释要求，现在一句「/review-pr」就搞定。更重要的是，这些 Skills 可以提交到项目的 `.claude/skills/` 目录，团队其他成员也能用上同样的标准。

* * *

## Part 6：进阶技巧（10 分钟）

### 6.1 技巧 1：Skills 与 Hooks 配合（3 分钟）

**\[展示 PPT：Hooks 配合\]**

Hooks 可以在特定事件时自动执行操作，结合 Skills 可以实现更强大的自动化。

**例如：代码保存后自动格式化**

```json
// .claude/hooks.json
{
  "hooks": {
    "PostToolUse": [{
      "matcher": {
        "tool_name": "Edit"
      },
      "hook": {
        "type": "command",
        "command": "/format-code"
      }
    }]
  }
}
```

这样每次编辑文件后，Claude Code 会自动调用 format-code skill 格式化代码。

### 6.2 技巧 2：团队协作（4 分钟）

**\[展示 PPT：团队协作\]**

**共享项目 Skills**：

1.  将 Skills 放在 `.claude/skills/` 目录
2.  提交到 Git 仓库
3.  团队成员克隆项目后即可使用

**版本控制**：

-   Skills 可以像代码一样进行版本控制
-   每个 commit 都可以记录 Skills 的变更
-   可以回滚到旧版本

**示例：团队 Git 工作流**

```bash
# 项目结构
my-project/
├── .claude/
│   └── skills/
│       ├── review-pr/
│       │   └── SKILL.md
│       ├── git-commit/
│       │   └── SKILL.md
│       └── gen-test/
│           └── SKILL.md
├── src/
└── README.md
```

团队成员克隆项目后，这三个 Skills 自动可用，确保所有人使用相同的代码审查标准、提交规范和测试生成流程。

### 6.3 技巧 3：调试 Skill（3 分钟）

**\[展示 PPT：调试技巧\]**

当 Skill 没有按预期工作时，小林总结了一套排查方法。

**常见问题排查**：

问题

可能原因

解决方案

Skill 没有被触发

description 不够具体

改进 description，包含具体的使用场景

YAML 解析错误

frontmatter 格式错误

检查 YAML 语法，确保缩进正确

技能不生效

Claude Code 没有重启

重启 Claude Code

找不到技能

目录位置错误

确认在 `~/.claude/skills/` 或 `.claude/skills/`

**调试步骤**：

1.  使用 `npx skills list` 查看技能是否被识别
2.  直接输入技能名称手动触发（如 `/review-pr`）
3.  检查 SKILL.md 文件内容是否正确
4.  查看 Claude Code 日志

**小林踩过的坑**：

她第一次创建的 Skill 一直不触发，后来发现是 description 写得太模糊了。原来写的是「审查代码」，改成「审查 Pull Request 的代码质量、安全性和测试覆盖率。当用户提到 PR、review、代码审查时触发」后就正常了。

关键是要在 description 里明确列出触发关键词。

* * *

## 本周回顾（5 分钟）

### 学习进度检查

**\[展示 PPT：ProgressTracker\]**

让我们回顾一下今天学到了什么：

✅ 理解了 Skills 是什么（知识和流程的打包） ✅ 会安装和使用 Skills（find-skills 搜索、安装社区 Skills） ✅ 掌握了 SKILL.md 结构（YAML frontmatter 和 Markdown 内容） ✅ 能创建自定义 Skills（两种方法创建 Skills） ✅ 跑通了实战案例（代码审查、Git 提交、测试生成） ✅ 理解了内部机制（三层加载架构、纯 LLM 路由）

### 自测问题

**\[展示 PPT：自测题\]**

1.  Skills 和提示词的核心区别是什么？为什么 Skills 更适合团队协作？
2.  SKILL.md 的 description 字段为什么这么重要？如何写出好的 description？
3.  Skills 和 MCP 的关系是什么？它们分别解决什么问题？
4.  三层渐进式加载架构是如何节省 Token 的？
5.  如何调试一个不触发的 Skill？排查顺序是怎样的？

### 本周作业

**\[展示 PPT：作业要求\]**

**必做**：

-   创建至少 3 个自定义 Skills
-   将 Skills 提交到项目的 `.claude/skills/` 目录
-   记录创建过程和遇到的问题

**选做**：

-   尝试将 Skills 与 Hooks 配合使用
-   探索社区 Skills，安装并体验至少 5 个
-   分享你的 Skills 到课程讨论区

**截止时间**：下周上课前

* * *

## 下周预告（3 分钟）

**\[展示 PPT：下周预告\]**

下周我们要学什么？

**Week 10：MCP——连接外部世界**

-   从本地到云端
-   从文件到服务
-   从单机到全栈

我们会学习如何给 Claude Code 接上 GitHub、数据库、浏览器等外部服务，让 AI 编程从「本地文件操作」扩展到「全栈工作流自动化」。

* * *

## Q&A（5 分钟）

**\[展示 PPT：Q&A\]**

现在是提问时间。大家有什么问题吗？

（回答学生问题）

* * *

## 结束语（2 分钟）

**\[展示 PPT：结束语\]**

今天我们学习了 Skills——给 Claude Code 装上「技能包」。

我希望大家记住三句话：

> Skills 是知识沉淀 一次创建，反复使用 团队共享，持续优化

不要担心做得不够好，不要害怕犯错。每个人都是从第一个 Skill 开始的。

期待下周看到你们的 Skills！

谢谢大家！

* * *

## 课后反思（教师用）

### 观察要点

-   学生是否都成功创建了至少 3 个 Skills？
-   哪些学生遇到了困难？是什么类型的困难？
-   学生对 Skills 的理解程度如何？
-   课堂互动是否充分？

### 改进建议

-   如果学生普遍卡在某个步骤，下次课可以提前准备更详细的演示
-   如果时间不够，可以压缩「进阶技巧」部分
-   如果学生反应热烈，可以增加更多实战案例

### 下周准备

-   提醒学生提前了解 MCP 的概念
-   准备 Week 10 的演示项目
-   收集本周作业，选出优秀 Skills 在下周展示
